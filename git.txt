// git and powershell commands  & tips
git --config global user.name

git --config global user.email

working diractory => staging area => git repository

git init 

git add

git commit -m 'your comment'

git log

git log --oneline

git checkout 'id' // switch between different versions

git add .

git clone 'repo address'

//connect local repo to remote repo 

git remote add origin 'your remote repo link'

// first push 

git push -u origin master

// get project last update from repo

git pull origin master

// branch lists

git branch

// create new branch 

git branch 'new branch name'

// switch in branches

git switch 'branch name'

